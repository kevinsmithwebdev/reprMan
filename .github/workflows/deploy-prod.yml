name: Deploy Production
on:
  push:
    branches:
      - production
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [24.x]

    env:
      NEXT_PUBLIC_REQUIRE_HOME_SIGN_IN: ${{ secrets.NEXT_PUBLIC_REQUIRE_HOME_SIGN_IN || vars.NEXT_PUBLIC_REQUIRE_HOME_SIGN_IN || secrets.VITE_REQUIRE_HOME_SIGN_IN || vars.VITE_REQUIRE_HOME_SIGN_IN || secrets.REACT_APP_REQUIRE_HOME_SIGN_IN || vars.REACT_APP_REQUIRE_HOME_SIGN_IN }}
      NEXT_PUBLIC_ALLOW_ANONYMOUS_HOME: ${{ secrets.NEXT_PUBLIC_ALLOW_ANONYMOUS_HOME || vars.NEXT_PUBLIC_ALLOW_ANONYMOUS_HOME || secrets.VITE_ALLOW_ANONYMOUS_HOME || vars.VITE_ALLOW_ANONYMOUS_HOME || secrets.REACT_APP_ALLOW_ANONYMOUS_HOME || vars.REACT_APP_ALLOW_ANONYMOUS_HOME }}
      NEXT_PUBLIC_COGNITO_IDENTITY_POOL_ID: ${{ secrets.NEXT_PUBLIC_COGNITO_IDENTITY_POOL_ID || vars.NEXT_PUBLIC_COGNITO_IDENTITY_POOL_ID || secrets.VITE_COGNITO_IDENTITY_POOL_ID || vars.VITE_COGNITO_IDENTITY_POOL_ID || secrets.REACT_APP_COGNITO_IDENTITY_POOL_ID || vars.REACT_APP_COGNITO_IDENTITY_POOL_ID }}

    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
        with:
          ref: production
          fetch-depth: 0

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node-version }}
          cache: yarn

      - name: Restore CI caches
        uses: ./.github/actions/restore-ci-cache

      - name: Install
        run: yarn install --frozen-lockfile

      - name: CI checks
        uses: ./.github/actions/ci-checks
        with:
          nx-base: HEAD~1

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Show AWS account
        run: aws sts get-caller-identity

      - name: Build server (Lambda + CDK bundle)
        run: yarn build:server

      - name: Deploy ReprServerStack-Prod
        working-directory: apps/server
        env:
          CDK_PROD_CORS_ORIGINS: ${{ vars.CDK_PROD_CORS_ORIGINS }}
          CDK_BILLING_ALERT_EMAIL: ${{ secrets.CDK_BILLING_ALERT_EMAIL }}
        run: |
          node ../../scripts/with-build-metadata.mjs APP "npx cdk deploy ReprServerStack-Prod -a \"node dist/cdk/app.js\" --require-approval never --outputs-file cdk-outputs.json"

      - name: Load Next.js env from CDK outputs
        working-directory: apps/server
        run: |
          set -e
          test -f cdk-outputs.json
          POOL=$(jq -r '.["ReprServerStack-Prod"].UserPoolId' cdk-outputs.json)
          CLIENT=$(jq -r '.["ReprServerStack-Prod"].UserPoolClientId' cdk-outputs.json)
          API=$(jq -r '.["ReprServerStack-Prod"].ApiBaseUrl' cdk-outputs.json)
          if [ -z "$POOL" ] || [ "$POOL" = "null" ]; then echo "::error::Missing UserPoolId in cdk-outputs.json"; exit 1; fi
          if [ -z "$CLIENT" ] || [ "$CLIENT" = "null" ]; then echo "::error::Missing UserPoolClientId in cdk-outputs.json"; exit 1; fi
          if [ -z "$API" ] || [ "$API" = "null" ]; then echo "::error::Missing ApiBaseUrl in cdk-outputs.json"; exit 1; fi
          {
            echo "NEXT_PUBLIC_COGNITO_USER_POOL_ID=$POOL"
            echo "NEXT_PUBLIC_COGNITO_USER_POOL_CLIENT_ID=$CLIENT"
            echo "NEXT_PUBLIC_REPRS_API_BASE_URL=$API"
          } >> "$GITHUB_ENV"

      - name: Build Next.js client (SSR bundle)
        run: yarn nx run client-web:build --skip-nx-cache

      - name: Build OpenNext deployment bundle
        run: yarn nx run client-web:build:open-next --skip-nx-cache

      - name: Resolve production bucket
        id: bucket
        run: |
          B="${{ secrets.AWS_PROD_BUCKET_NAME }}"
          if [ -z "$B" ]; then B="${{ secrets.AWS_TEST_BUCKET_NAME }}"; fi
          if [ -z "$B" ]; then
            echo "::error::Set secret AWS_PROD_BUCKET_NAME (or legacy AWS_TEST_BUCKET_NAME) for the production S3 bucket."
            exit 1
          fi
          echo "name=$B" >> "$GITHUB_OUTPUT"

      - name: Verify S3 bucket exists
        env:
          BUCKET: ${{ steps.bucket.outputs.name }}
          REGION: ${{ secrets.AWS_REGION }}
        run: aws s3 ls "s3://$BUCKET/" --region "$REGION"

      - name: Deploy static assets to S3
        env:
          BUCKET: ${{ steps.bucket.outputs.name }}
          REGION: ${{ secrets.AWS_REGION }}
        run: |
          aws s3 sync apps/client-web/.open-next/assets "s3://$BUCKET/" \
            --region "$REGION" \
            --delete \
            --cache-control "public,max-age=31536000,immutable"

      - name: Deploy OpenNext server bundle
        env:
          REGION: ${{ secrets.AWS_REGION }}
          OPEN_NEXT_LAMBDA: ${{ secrets.AWS_OPEN_NEXT_LAMBDA_NAME }}
        run: |
          if [ -z "$OPEN_NEXT_LAMBDA" ]; then
            echo "::warning::AWS_OPEN_NEXT_LAMBDA_NAME is not set; skipping Lambda code update."
            echo "Configure a Lambda (OpenNext server handler) and set AWS_OPEN_NEXT_LAMBDA_NAME to enable SSR in production."
            exit 0
          fi
          (cd apps/client-web/.open-next/server-function && zip -r ../server-function.zip .)
          aws lambda update-function-code \
            --function-name "$OPEN_NEXT_LAMBDA" \
            --zip-file fileb://apps/client-web/.open-next/server-function.zip \
            --region "$REGION"

      - name: Invalidate CloudFront cache
        env:
          DIST: ${{ secrets.AWS_PROD_CLOUDFRONT_DISTRIBUTION_ID }}
          LEGACY: ${{ secrets.AWS_CLOUDFRONT_DISTRIBUTION_ID }}
        run: |
          D="${DIST:-$LEGACY}"
          if [ -z "$D" ]; then
            echo "No AWS_PROD_CLOUDFRONT_DISTRIBUTION_ID or AWS_CLOUDFRONT_DISTRIBUTION_ID; skipping invalidation."
            exit 0
          fi
          aws cloudfront create-invalidation --distribution-id "$D" --paths "/*"
